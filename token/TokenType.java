package token;

public enum TokenType {
    NUMBER,
    STRING,
    IDENTIFIER,

    PLUS,
    MINUS,
    STAR,
    SLASH,

    GREATER,
    LESS,
    EQUAL_EQUAL,

    PUT,
    INTO,
    PRINT,
    IF,
    THEN,
    REPEAT,
    TIMES,
    COLON,

    TAB,
    NEWLINE,
    EOF
}